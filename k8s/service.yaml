apiVersion: v1
kind: Service
metadata:
  name: simple-server
spec:
  selector:
    app: simple-server
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      name: eighty-eighty
    - protocol: TCP
      port: 9000
      targetPort: 9000
      name: nine-thousand
